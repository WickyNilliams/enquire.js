/*global describe: true, beforeEach: true, afterEach: true, it: true, expect: true */

describe("Specs for MediaQueryDispatch type", function() {

	beforeEach(function() {
		//setup
	});

	afterEach(function() {
		//teardown
	});

	it("throws if matchMedia is not present", function() {

	});

	it("allows new media queries to be registered", function() {
		
	});

	it("allows arrays of media queries to be registered", function() {
		
	});

	it("will call match() on each media query that matches, when fired", function() {
		
	});

	it("will call unmatch() on each media query that doesn't match, when fired", function() {
		
	});

	it("will listen for browser events", function() {
		
	});
	
});
