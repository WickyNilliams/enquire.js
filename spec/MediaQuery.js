/*global describe: true, beforeEach: true, afterEach: true, it: true, expect: true */

describe("Specs for the MediaQuery type", function() {

	beforeEach(function() {
		//setup
	});

	afterEach(function() {
		//teardown
	});

	it("checks whether the query matches on construction", function() {

	});

	it("can accept new handlers", function() {

	});

	it("turns on new handler if query is already matching", function() {
		
	});

	it("turns on all handlers when matching", function() {
		
	});

	it("doesn't turn on handlers if already matching", function() {
		
	});

	it("sets matched to true when match is called", function() {
		
	});

	it("turns off all handlers when not matching", function() {
		
	});

	it("doesn't turn off handlers if already not matching", function() {
		
	});

	it("sets matched to false when unmatch is called", function() {
		
	});

});
